---
- name: Allow JumpServer service port
  firewalld:
    port: '{{ item.value }}/tcp'
    zone: 'public'
    permanent: 'true'
    immediate: 'true'
    state: 'enabled'
  loop: '{{ jumpserver_port_arg|dict2items }}'
